# 🔧 رفع مشکلات رایج

## Cloudflare Workers

### مشکل: Worker deploy نمیشه

**علت‌های احتمالی:**
1. Account ID اشتباه
2. مشکل احراز هویت
3. مشکل اینترنت

**راه‌حل:**
```bash
# خروج و ورود مجدد
wrangler logout
wrangler login

# بررسی account ID
wrangler whoami

# deploy مجدد
wrangler deploy
```

### مشکل: اتصال برقرار نمیشه

**چک‌لیست:**
- [ ] Worker deploy شده؟ (`wrangler deployments list`)
- [ ] URL درست وارد شده؟
- [ ] از HTTPS استفاده میکنید؟ (نه HTTP)
- [ ] تنظیمات Proxy صحیح است؟

**تست اتصال:**
```bash
curl https://your-worker.workers.dev/health
```

باید جواب بده: `{"status":"ok",...}`

### مشکل: سرعت پایین

**راه‌حل:**
1. Cache رو فعال کنید (در worker.js)
2. از Region نزدیک‌تر استفاده کنید
3. حجم request ها رو کاهش بدید

---

## GitHub Codespaces

### مشکل: Codespace شروع نمیشه

**راه‌حل:**
1. صبر کنید (گاهی 2-3 دقیقه طول میکشه)
2. مرورگر رو refresh کنید
3. Codespace رو delete و دوباره بسازید

### مشکل: اسکریپت اجرا نمیشه

**خطا: Permission denied**
```bash
chmod +x setup-xray.sh
./setup-xray.sh
```

**خطا: Command not found**
```bash
bash setup-xray.sh
```

### مشکل: VPN وصل نمیشه

**چک‌لیست:**
- [ ] Codespace روشنه؟
- [ ] Xray در حال اجراست؟ (`ps aux | grep xray`)
- [ ] لینک اشتراک درست کپی شده؟
- [ ] Port درست است؟ (8080)

**مشاهده لاگ:**
```bash
cat ~/xray/xray.log
```

### مشکل: اتصال قطع میشه

**علت:** Codespace بعد 30 دقیقه idle خاموش میشه

**راه‌حل:**
1. دوباره Start کنید
2. یا از این اسکریپت استفاده کنید:
```bash
# Keep-alive (استفاده نشه - هدر رفتن ساعات!)
while true; do curl localhost:8080/health; sleep 60; done
```

### مشکل: آدرس سرور عوض شد

**علت:** بعد از هر restart آدرس تغییر میکنه

**راه‌حل:**
```bash
# اسکریپت رو دوباره اجرا کنید
cd github-codespaces
./setup-xray.sh

# لینک جدید رو کپی و در کلاینت آپدیت کنید
```

### مشکل: ساعات تموم شد

**چک استفاده:**
[github.com/settings/billing](https://github.com/settings/billing)

**راه‌حل:**
1. منتظر ماه بعد بمونید
2. اکانت دیگه بسازید
3. به Cloudflare Workers برگردید

---

## مشکلات کلاینت

### v2rayNG (اندروید)

**مشکل: Import نمیشه**
- لینک کامل رو کپی کنید (شامل `vmess://`)
- نسخه جدید v2rayNG نصب کنید
- Manual اضافه کنید

**مشکل: Connected ولی اینترنت نداره**
- Routing رو چک کنید
- DNS رو عوض کنید (1.1.1.1)
- چک کنید VPN permission داده باشید

### v2rayN (ویندوز)

**مشکل: Cannot connect**
- برنامه رو Run as Administrator کنید
- Firewall رو چک کنید
- Antivirus رو موقتاً خاموش کنید

### Shadowrocket (iOS)

**مشکل: VPN profile install نمیشه**
- Settings → General → VPN & Device Management
- Trust profile
- دوباره امتحان کنید

---

## مشکلات عمومی

### پینگ بالا

**بهبود:**
1. Region سرور رو تغییر بدید
2. از DNS سریع‌تر استفاده کنید
3. Protocol رو عوض کنید

### سرعت کند

**چک‌لیست:**
- [ ] حجم ماهانه تموم نشده؟
- [ ] سرور overload نیست؟
- [ ] اینترنت اصلی شما خوبه؟

### مشکل با سایت خاص

**راه‌حل:**
1. Cache مرورگر رو پاک کنید
2. Cookie ها رو پاک کنید
3. از مرورگر incognito استفاده کنید

---

## کمک بیشتر

اگه مشکل حل نشد:

1. لاگ‌ها رو چک کنید
2. GitHub Issues رو بخونید
3. سوالتون رو بپرسید

**لاگ‌های مهم:**
- Cloudflare: `wrangler tail`
- Xray: `~/xray/xray.log`
- کلاینت: در تنظیمات کلاینت

---

**99% مشکلات با این راهنما حل میشه! 🎯**
