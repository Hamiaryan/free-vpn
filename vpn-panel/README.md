# 🎨 پنل مدیریت VPN

یک پنل مدیریت حرفه‌ای برای ساخت و مدیریت کانفیگ‌های VPN با امکانات زیر:

## ✨ امکانات

- ✅ ساخت کانفیگ‌های VMess/VLESS/Trojan
- ✅ تنظیم محدودیت ترافیک (10GB, 50GB, 100GB, نامحدود)
- ✅ تنظیم تاریخ انقضا (7, 30, 90, 365 روز)
- ✅ نمایش مقدار مصرف realtime
- ✅ نمایش تاریخ باقیمانده
- ✅ تولید QR Code خودکار
- ✅ کپی لینک با یک کلیک
- ✅ دانلود QR Code
- ✅ UI مدرن و responsive
- ✅ Dark mode با Glassmorphism
- ✅ ذخیره در Cloudflare D1 Database
- ✅ API کامل با Cloudflare Workers

## 🚀 راه‌اندازی

### 1. نصب Wrangler

```bash
npm install -g wrangler
wrangler login
```

### 2. ساخت D1 Database

```bash
# ساخت دیتابیس
wrangler d1 create vpn-configs

# کپی database_id از خروجی و جایگذاری در wrangler.toml

# اجرای migration
wrangler d1 execute vpn-configs --file=schema.sql
```

### 3. آپدیت wrangler.toml

فایل `wrangler.toml` را باز کنید و `database_id` را با ID دیتابیس جایگزین کنید.

### 4. آماده‌سازی فایل‌های Static

```bash
# ساخت پوشه public
mkdir -p public
cp index.html styles.css app.js public/
```

### 5. Deploy

```bash
wrangler deploy
```

بعد از deploy، یک URL دریافت می‌کنید مثل:
```
https://vpn-panel.your-username.workers.dev
```

## 📱 استفاده

### ساخت کانفیگ جدید

1. کلیک روی "ساخت کانفیگ جدید"
2. نام کانفیگ را وارد کنید
3. محدودیت ترافیک را انتخاب کنید
4. مدت اعتبار را انتخاب کنید
5. پروتکل را انتخاب کنید
6. کلیک روی "ساخت کانفیگ"

### استفاده از کانفیگ

1. روی کانفیگ کلیک کنید
2. QR Code را با موبایل اسکن کنید
3. یا لینک را کپی کنید و در اپ import کنید

### اپلیکیشن‌های پشتیبانی شده

- **اندروید**: v2rayNG
- **iOS**: Shadowrocket, Quantumult X
- **ویندوز**: v2rayN, Clash
- **مک**: ClashX, V2RayX

## 🔧 تنظیمات پیشرفته

### اتصال به Xray Server

فایل `wrangler.toml` را ویرایش کنید:

```toml
[vars]
XRAY_SERVER = "your-server.com"
XRAY_PORT = "443"
XRAY_PATH = "/xray"
```

### آپدیت کردن مصرف

برای tracking مصرف، می‌توانید از API استفاده کنید:

```javascript
fetch('https://your-worker.workers.dev/api/configs/{id}/usage', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ used: 1024000000 }) // bytes
});
```

## 📊 API Endpoints

### GET /api/configs
دریافت لیست تمام کانفیگ‌ها

### POST /api/configs
ساخت کانفیگ جدید

### DELETE /api/configs/{id}
حذف کانفیگ

### POST /api/configs/{id}/usage
آپدیت مصرف

### GET /api/stats
دریافت آمار کلی

## 🎨 سفارشی‌سازی UI

### تغییر رنگ‌ها

فایل `styles.css` را ویرایش کنید:

```css
:root {
    --primary: #667eea; /* رنگ اصلی */
    --secondary: #764ba2; /* رنگ ثانویه */
    /* ... */
}
```

### تغییر لوگو

SVG لوگو در `index.html` را جایگزین کنید.

## 🔒 امنیت

- همه ارتباطات از طریق HTTPS
- UUID‌های random برای هر کانفیگ
- عدم ذخیره هیچ اطلاعات شخصی
- ذخیره امن در D1 Database

## 📝 لایسنس

MIT License - استفاده آزاد

## 🤝 مشارکت

Pull request‌ها خوشامد هستند!

## 📞 پشتیبانی

در صورت مشکل، Issue باز کنید:
https://github.com/Hamiaryan/free-vpn/issues

---

**ساخته شده با ❤️ برای اینترنت آزاد**
