# 🚀 Cloudflare Workers Proxy

## نصب و راه‌اندازی (5 دقیقه!)

### پیش‌نیازها
- یک اکانت رایگان Cloudflare ([ثبت‌نام](https://dash.cloudflare.com/sign-up))
- Node.js نصب شده (برای Wrangler CLI)

### مراحل نصب

#### گام 1: نصب Wrangler CLI

```bash
npm install -g wrangler
```

#### گام 2: لاگین به Cloudflare

```bash
wrangler login
```

یک صفحه مرورگر باز میشه، لاگین کنید و اجازه بدید.

#### گام 3: پیدا کردن Account ID

1. برید به [Cloudflare Dashboard](https://dash.cloudflare.com)
2. از سمت راست، روی Workers & Pages کلیک کنید
3. Account ID رو کپی کنید

#### گام 4: ویرایش wrangler.toml

فایل `wrangler.toml` رو باز کنید و `account_id` رو پر کنید:

```toml
account_id = "your-account-id-here"
```

#### گام 5: Deploy!

```bash
wrangler deploy
```

### تبریک! 🎉

Worker شما آماده است. یک آدرس مثل این دریافت میکنید:

```
https://free-vpn-proxy.your-subdomain.workers.dev
```

---

## 📖 نحوه استفاده

### روش 1: مستقیم در مرورگر

```
https://your-worker.workers.dev/?url=https://google.com
```

### روش 2: تنظیم به عنوان Proxy

در تنظیمات شبکه سیستم‌عامل یا مرورگر:

- **نوع:** HTTPS Proxy
- **سرور:** `your-worker.workers.dev`
- **پورت:** `443`

### روش 3: استفاده در تلگرام

1. باز کردن تلگرام
2. تنظیمات → داده و حافظه → استفاده از پروکسی
3. افزودن پروکسی:
   - نوع: HTTP
   - سرور: `your-worker.workers.dev`
   - پورت: 443

---

## 🔧 سفارشی‌سازی

### محدود کردن به دامنه‌های خاص

در `worker.js`، خط 13 را ویرایش کنید:

```javascript
allowedDomains: ['google.com', 'youtube.com', 'twitter.com'],
```

### غیرفعال کردن Cache

```javascript
enableCache: false,
```

### تغییر مدت Cache

```javascript
cacheTTL: 7200, // 2 ساعت
```

---

## 📊 محدودیت‌های رایگان

Cloudflare Workers Free Tier:
- ✅ **100,000 requests** روزانه
- ✅ **10ms CPU time** per request
- ✅ **نامحدود** bandwidth
- ✅ **رایگان برای همیشه**

برای استفاده شخصی کاملاً کافیه! 🎉

---

## 🐛 رفع مشکلات

### خطا: "Module not found"
```bash
npm install -g wrangler@latest
```

### خطا: "Authentication failed"
```bash
wrangler logout
wrangler login
```

### Worker deploy نمیشه
- چک کنید `account_id` رو درست وارد کرده باشید
- مطمئن شوید اینترنت دارید
- از VPN برای لاگین به Cloudflare استفاده کنید

---

## 🔒 نکات امنیتی

- ✅ تمام ترافیک HTTPS رمزنگاری شده
- ✅ Cloudflare هیچ لاگی ذخیره نمیکنه (طبق Privacy Policy)
- ✅ کد منبع open source و قابل بررسی
- ⚠️ برای کارهای حساس از VPN کامل استفاده کنید

---

## 💡 نکات استفاده

1. **برای وب سرفینگ عادی** این بهترین گزینه است (سریع و رایگان)
2. **برای تلگرام و اینستاگرام** عالی کار میکنه
3. **برای یوتیوب** بدون مشکل استریم میکنه
4. **برای بازی** ممکنه latency بالا باشه

---

## 📈 بهبودهای آینده

- [ ] پشتیبانی از WebSocket
- [ ] افزودن authentication
- [ ] Dashboard مدیریت
- [ ] آمار استفاده
- [ ] چند Region برای سرعت بهتر

---

## 🤝 مشارکت

Pull request ها و پیشنهادات خوشحال میشم!

---

**ساخته شده با ❤️ برای دسترسی آزاد به اینترنت**
